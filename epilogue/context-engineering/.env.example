# Required API keys
OPENAI_API_KEY=your_openai_api_key

# Optional LangSmith (for Studio auth to remote servers)
LANGSMITH_API_KEY=your_langsmith_api_key
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=yelp-navigator-LangGraph-Local

# # Models/config
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Default model for all services (OpenAI or Ollama)
# Examples: gpt-4o-mini, gpt-oss:latest, deepseek-r1:latest, qwen3:latest
DEFAULT_MODEL=gpt-4o-mini

# Test model override (used by test_models.py)
# When set, overrides DEFAULT_MODEL for testing purposes
# Leave empty to use DEFAULT_MODEL
TEST_MODEL=